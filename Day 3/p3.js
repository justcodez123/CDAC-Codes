// Declare an array with string data
const colors = ["red", "green", "blue", "yellow"];

// Display every element in uppercase format
console.log("--- Colors in Uppercase ---");
colors.forEach(color => {
    console.log(color.toUpperCase());
});